# turing
Turing Pattern Simulator - Streamlit App

1) `git clone https://github.com/aclcheuk/turing.git`
2) `conda env create -f environment.yml`
3) `conda activate turing-env`
4) `streamlit run app.py`
5) `conda deactivate`
